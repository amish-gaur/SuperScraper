services:
  api:
    build: .
    command: uvicorn api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - .env.local
    environment:
      ARTIFACT_ROOT: /app/artifacts
      APP_ENV: production
    volumes:
      - .:/app
